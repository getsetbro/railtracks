import { DrawingDirective } from './drawing.directive';

describe('DrawingDirective', () => {
  it('should create an instance', () => {
    const directive = new DrawingDirective();
    expect(directive).toBeTruthy();
  });
});
